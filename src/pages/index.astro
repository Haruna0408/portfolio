---
import Layout from "../layouts/Layout.astro";

//microCMS呼び出し
import { getPortfolio } from "../library/microcms";
const response = await getPortfolio({ fields: ["id", "title"] });
---

<Layout title="My first portfolio with Astro">
  <main>
    <ul>
      {
        response.contents.map((content: any) => (
          <li>
            <a href={content.id}>{content.title}</a>
          </li>
        ))
      }
    </ul>
  </main>
</Layout>

<style>
  main {
    margin: auto;
    padding: 1em;
    max-width: 60ch;
  }
</style>